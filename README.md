# public-site

公開用 staging ディレクトリ。

- `workspace/user-profile/current/` などの private 側成果物から、公開してよいものだけを明示的に export する
- このディレクトリの内容だけを public repository へ同期する想定
- 通常運用では private repository 側だけを更新し、公開したいタイミングで export と確認を行う
